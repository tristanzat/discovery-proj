apiVersion: v1
kind: Service
metadata:
  name: dungeon-crawler-api
  namespace: dungeon-crawler
spec:
  selector:
    app: dungeon-crawler-api
  ports:
    - name: http
      port: 80
      targetPort: 8080